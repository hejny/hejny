import { ChaosCanvas } from '../components/ChaosCanvas/ChaosCanvas';
import { AppHead } from '../sections/00-AppHead/AppHead';

export default function AiTrainingPage() {
    return (
        <>
            <AppHead subtitle="BAB" />
            <ChaosCanvas />
        </>
    );
}
